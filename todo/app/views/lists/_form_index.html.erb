<%= form_with(model: @list) do |form| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
        <% list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

	  <div class="d-flex">
	    <%= form.text_field :description, class: "form-control me-2", placeholder: "Add a new item" %>
	    <%= form.submit "Add", class: "btn btn-sm btn-outline-success" %>
	  </div>

<% end %>
